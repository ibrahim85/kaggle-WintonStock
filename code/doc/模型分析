1.我们利用TS技术发现并不能完全整合好我们的模型，太大的误差
所以我们建议不能使用TS预测

2.我们使用Lasso，Ridge预测的时候，Lasoo,可以给我们一个很好的预测，但是
这种预测是一种健壮的预测，将所有的参数都设为0了，但是缺获得很好的预测结果，虽然他的
结果不能打破benchmark，但是给我们的提示就是这个模型越简单越好
the 0 benchmark suppose 896.0831361676281

#使用Ridge回归时，我们预测是可以获得很好的结果的，但是发现CV没有效果，则我们可以得到以下
的结果，则是我们设计的train test data是来自不同的模型，或者是不同的时间
因此利用回归的方式并不能很好解决我们问题，因此我们需要利用简单的模型来建立我们的模型

下面是对数据的分析情况一个比较
当我们进入0 benchmark的时候，本地local CV值应该为1773.924,pulic Benchmark is 1770.0321
而且通过一定比例计算，daily error 在benchmark中应该是896.0831361676281
而我们利用 lasso CV的结果去预测的时候，有如下的结果
而通过lasso CV结果是差不多我们的结果应该是 889.2502931602565  预计通过比例（0.9978058345024042）就算应该是891.2057460595194

#rate 1.015174769190492
